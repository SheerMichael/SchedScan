# Core Django and REST API
Django==5.2.7
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.3.1

# Database
psycopg2-binary==2.9.11

# Utilities
python-dotenv==1.1.1

# Production deployment
gunicorn==21.2.0
whitenoise==6.6.0

# PDF Text Extraction (Primary Method)
pdfplumber==0.11.0
tabulate==0.9.0
Pillow==10.1.0

# ============================================
# OCR Dependencies (OPTIONAL - Heavy ~2GB)
# ============================================
# Only install these locally if you need OCR fallback:
# pip install python-doctr[torch]==0.8.1 opencv-python-headless==4.8.1.78
#
# Note: These require system libraries (libGL) that may not be
# available on cloud platforms. PDF extraction via pdfplumber
# is the primary method and works for most COR documents.
